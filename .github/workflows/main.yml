name: A workflow for my Hello World file
on: push

jobs:
  build:
    name: Hello world action
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: docker build
      run: docker build -t test ./action-a
    - name: docker run
      run: docker run -e MY_NAME=Mona --rm test
